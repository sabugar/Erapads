<?xml version="1.0" encoding="utf-8"?>

<odoo>
    <record id="sabugar_sale_order_tree_filter" model="ir.ui.view">
        <field name="name">sabugar.sale.order.filter</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.sale_order_view_search_inherit_sale"/>
        <field name="arch" type="xml">
            <data>
               <xpath expr="/search/filter[1]" position="before">
                    <filter string="Today" name="today_sale_filter"/>
                    <filter string="Yesterday" name="yesterday_sale_filter"/>
                    <filter string="This Month" name="this_month_sale_filter"/>
                    <separator/>
                </xpath> 
            </data>
        </field>
    </record>
</odoo>